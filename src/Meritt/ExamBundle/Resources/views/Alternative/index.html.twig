{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Alternative list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Alternativetext</th>
                <th>Iscorrect</th>
                <th>Alternativelabel</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('alternative_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.alternativeText }}</td>
                <td>{{ entity.isCorrect }}</td>
                <td>{{ entity.alternativeLabel }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('alternative_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('alternative_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('alternative_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
